<% @page_title = "Delete Subject" %>
<%= link_to("<< Back to List", {:action => 'index'}, :class => 'back-link') %>

<div class="Subjects delete">
	<h2>Delete Subject</h2>

	<%= form_for(:admin_user, :url => {:action => 'destroy', :id => @AdminUser}) do %>

	<p>Are you sure you want to permanently delete this admin user?</p>

	<p class="reference-name"><%= @AdminUser.username %></p>

	<div class="form-buttons">
		<%= submit_tag("Delete Admin User") %>
	</div>

	<% end %>
</div>